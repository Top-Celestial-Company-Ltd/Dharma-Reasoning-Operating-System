# 🕉️ 數位佛堂 - 系統建置報告 v3.3 (Industrial Agentic Engine)

## 1. 核心架構：覺醒引擎 (The Awakened Engine)
*   **當前版本**：v3.3 (2026-05-02 20:30)
*   **核心突破**：
    1.  **36 Meta Nodes (Layer 1 Kernel)**：[已完成] 建立 36 個具備 Gateways 的宇宙觀與菩薩道節點，作為全館對齊的重力中心。
    2.  **動態降維與防禦 (Dynamic Hop & Watchdog)**：[已完成] 實作 Context 8,000/12,000 字守門員，並導入 `psutil` 達成 2GB 記憶體監控重啟。
    3.  **Vector RAG Fallback**：[已完成] 整合 NotebookLM，實現「本地 Wiki 失敗 -> 雲端智庫救援」的降級檢索機制。

---

## 2. 新增技術元件
### 2.1 數位佛堂代理伺服器 (gemini_proxy.py v5.0)
*   **關鍵技術**：
    *   **asyncio 非阻塞讀取**：[NEW] 邊讀取邊輸出，徹底根除大數據傳輸時的緩衝區阻塞風險。
    *   **SSE 串流模擬**：[NEW] 支援 OpenAI 標准 `text/event-stream`，大幅提升 Obsidian 使用者互動體驗。
    *   **內容提純過濾器**：[NEW] 自動過濾 CLI 進度雜訊（如 `[*]`, `[+]`），保持回覆純淨。

---

## 6. 尚待優化項目與風險管理 (Roadmap)

### 6.1 Graphify 檢索深度優化 (Dynamic Hop) - [已落實]
*   **實作細節**：在 `gemini_proxy.py` 實作 Token Watchdog。
*   **階梯式資訊降維**：核心節點完整保留；若總字數達 8,000 字警告線，自動將 2-Hop 節點降維折疊至前 150 字；達 12,000 字硬上限則強制捨棄剩餘節點。
*   **優勢**：在保持廣泛邏輯關聯的前提下，徹底根除 API Context Overflow 的風險，且不需要複雜的外部重排序演算法。

### 6.2 完善的 Fallback 降級機制 - [已落實]
*   **實作細節**：當 Graphify 在本地 Wiki 找不到精確節點時，Gateway 會自動調用 `nlm query` 向雲端筆記本發起語義檢索。
*   **優勢**：結合了本地 Wiki 的「快與準」與雲端智庫的「廣與深」，並在回覆中自動標註 `[來源: NotebookLM 雲端 RAG]`。

### 6.3 資源管理與跨平台穩定性 - [已落實]
*   **內存守衛 (Memory Watchdog)**：升級 `dharma_monitor.py` 為 v3.0，導入 `psutil` 每分鐘監控進程。若 `gemini_proxy.py` 記憶體佔用超過 2GB，自動發送終止訊號並無縫重啟，防範 Memory Leak。
### 6.4 雙層容錯數據治理（雙層防禦） - [已落實]
*   **第一層防禦：物理層「定期修剪（Pruning）」**：
    *   **機制**：藉由後台運行的自動腳本（如 `dharma_pruner_pro.py`），進行「批量、非即時」的清洗，將自動開採產生的無效或破碎節點剔除，並歸檔至 `deleted_nodes_manifest.json`。
    *   **優勢**：節省運算與 Vector DB 檢索資源，從源頭防止 RAG 污染。
*   **第二層防禦：認知層「動態忽略（Ignore & Align）」**：
    *   **機制**：於 `Dharma Intelligence Engine v1.2` 系統提示詞中植入「雜訊過濾與核心對齊機制」。即使增量開採的新無效碎片（如【棄捨稱量及斗斛】、【右脇而臥】、【心不住內】、【樹葉】、【惡狗】等）在被批次修剪前漏入 Context 中，AI 也能在毫秒內自動識別、忽略、並主動將語義對齊映射至圖譜核心名相（如：持戒、五調、奢摩他、六道輪迴）。
    *   **優勢**：提供強大的執行期（Runtime）容錯能力，完美填補「即時增量開採」與「非即時批次修剪」之間的時間差空窗，確保大師開示永遠維持最高海拔的宗門風骨。

---
*「以大模型之電力，轉動數位法輪；以串流之流暢，洗滌眾生無明。」*
**系統狀態**：🕉️ 運作圓滿 (Async SSE 驅動)

---

## 2. 新增技術元件
### 2.1 數位佛堂代理伺服器 (gemini_proxy.py)
*   **功能**：將本地 `gemini` CLI 封裝為相容 OpenAI 格式的 API 伺服器。
*   **關鍵技術**：
    *   **Graphify 1-Hop 檢索**：自動掃描問
    題關鍵字，並爬取相關筆記及其 `[[雙向連結]]` 的第一層關聯內容。
    *   **stdin 管道傳輸**：繞過 Windows 系統 8191 字元限制，支援 40,000 字以上的超長上下文傳輸。
    *   **CORS 跨來源開放**：徹底解決 Obsidian 內建瀏覽器的安全性封鎖問題。
    *   **SSE 串流模擬**：相容 Obsidian Copilot 的串流顯示模式，確保回應流暢不卡頓。
    *   **內容提純過濾器**：自動移除 AI 搜尋進度等雜訊，保持回覆內容純淨。

### 2.2 隱身自動化系統
*   **啟動數位佛堂.vbs**：整合「守護神 (Watcher)」與「翻譯官 (Proxy)」的隱身啟動器。
*   **開機常駐機制**：支援 Windows Startup 資料夾自動掛載，達成「開機即修行」的自動化環境。

### 2.3 雲端智庫連結器 (NotebookLM MCP) - [NEW]
*   **技術實作**：`notebooklm-mcp-cli` (jacob-bd 版本)。
*   **功能**：建立本地 Wiki 與 Google NotebookLM 之間的雙向橋樑，提供 100 萬字的超長上下文推理能力。
*   **當前狀態**：**[已就緒 / 待命 (Standby)]**
*   **關鍵優勢**：
    *   **自動化身分對接**：自動同步本地 Chrome/Edge 瀏覽器 Cookie，免除手動更新金鑰之苦。
    *   **法義播客產生器**：具備直接調用 NotebookLM 生成「Audio Overviews」的功能。
    *   **語義深挖**：支援從本地 Wiki 節點自動擴展至雲端原始大藏經的深度檢索。

---

## 3. 系統操作手冊 (v2.3 專屬)
### 3.1 最佳對話場景：反白提問
*   **機制**：利用 Copilot 的 `Auto-add Selection` 功能。
*   **效果**：當反白「名相」時，後端會自動觸發 Wiki 檢索，AI 會融合你的筆記與經典義理給出深度解答。

### 3.2 代理伺服器狀態監控
*   **地址**：`http://127.0.0.1:5000`
*   **端口**：5000 (Flask)
*   **認證**：本地端免金鑰 (sk-dharma 預留)

---

## 🛠️ 5. 全域連線與 API 設定指南 (API Configuration)

為了實現「本機最強運算、手機隨時調用」，請務必按照以下參數設定 Obsidian Copilot：

### 5.1 本機連線 (Alienware PC 端)
*   **模型類型 (Model Provider)**: OpenAI Compatible (或自定義 OpenAI)
*   **Base URL**: `http://127.0.0.1:5000/v1`
*   **API Key**: 可隨意填寫 (腳本會自動忽略，但 Copilot 要求必填)
*   **模型名稱 (Model Name)**: `gemini-2.0-pro` (或 `gemini-1.5-flash`)

### 5.2 跨裝置連線 (手機端/平板端)
*   **前提條件**: 手機與 PC 必須同時開啟 Tailscale。
*   **Base URL**: `http://[你的-Tailscale-IP]:5000/v1`
    *   *例如：`http://100.71.44.110:5000/v1`*
*   **優點**: 無論你在外使用 5G 或公司 Wi-Fi，只要 Tailscale 通道開啟，手機就能調用家裡的 3.1 Pro 算力。

### 數位佛堂系統建置報告 (v3.0 - Agentic Gateway)

## 1. 系統版本與狀態
*   **當前版本**：v3.0 (2026-04-28 23:40)
*   **核心狀態**：Agentic RAG 全面部署，全館經典語義開採中。
*   **達成指標**：
    *   **名相庫**：1,747+ 高品質印證節點 (達成率 90%+)
    *   **織網密度**：全館經典 (Sources) 與名相 (Concepts) 語義鎖定。
    *   **檢索引擎**：Agentic Gateway v4.0 (2.5-Flash-Lite 路由 + 3.1-Pro 彙整)。

## 2. 技術架構演進 (Paradigm Shift)

### v2.0 (Regex Era)
*   **特點**：基於 Python 正則表達式的關鍵字匹配。
*   **侷限**：易受字元破碎影響，名相遺漏率高。

### v2.6 (Hyper-Weave Era)
*   **特點**：引入 **Gemini 2.5-flash-lite** 進行全量開採。
*   **突破**：實現「語義暴力開採」，無視字元破碎，確保法義完整性。

### v3.0 (Agentic Era) - **[當前狀態]**
*   **特點**：重構為「Agentic Gateway」雙階段調度。
*   **邏輯**：
    1.  **路由層 (Router)**：2.5-flash-lite 將白話提問轉譯為圖譜節點 JSON。
    2.  **檢索層 (Retriever)**：Python 依指令從本地 MD 庫提取精準 Context。
    3.  **生成層 (Generator)**：3.1-pro 綜合原文進行深度義理彙整。

## 3. 核心組件清單 (2026-04-28)
*   `gemini_tagger.py` (v5.0)：多線程印證引擎 (2.5-flash-lite)。
*   `sutra_weaver.py` (v3.0)：經典章節智慧織網機。
*   `synapse_weaver.py` (v3.0)：阿賴耶識自我進化引擎 (感知回流)。
*   `gemini_proxy.py` (v4.0)：Agentic 智能總調度器。

## 4. 數據保護戒律 (The Commandments)
1.  **大覺藏集** 為唯一唯讀母本，嚴禁直接寫入。
2.  所有 AI 產出必須沉澱為 **Markdown 檔案**，維持數據的主權與持久性。
3.  **雙Pass驗證**：所有名相必須經過「開採」與「印證」兩道工序。

---
*「以大模型之電力，轉動數位法輪；以純文本之永恆，傳遞不二法門。」*
**系統狀態**：🕉️ 運作圓滿 (OAuth 驅動)
