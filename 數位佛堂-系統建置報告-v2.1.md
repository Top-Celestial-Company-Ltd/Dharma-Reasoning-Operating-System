# 🏛️ 數位佛堂 v2.1 — 系統建置完成報告

> **建置日期**：2026-04-24
> **建置狀態**：✅ 生產就緒 (Production Ready)
> **系統代號**：數位佛堂 閉環演化機制 v2.1

---

## 一、系統架構總覽

本系統實現「聞、思、修」三位一體的知識自動演化機制，
將傳統「死資料、活檢索」的 RAG 模式，升級為「**活資料、導向式檢索**」。

```
┌─────────────────────────────────────────────────────┐
│                  數位佛堂 閉環機制                   │
│                                                     │
│  聞思層   Obsidian + Smart Connections              │
│           ↓  對話激發 → 原子節點 .md                │
│                                                     │
│  人工印證  /inbox/  ← 架構師 Jimmy 審閱             │
│           ↓  確認後拖入 /concepts/                  │
│                                                     │
│  修證層   dharma_watcher.py  (watchdog 即時偵測)    │
│           ↓  偵測到新檔 → 5 秒冷卻防抖 → 觸發      │
│                                                     │
│  織網層   synapse_weaver.py v2.0                    │
│           ↓  全庫掃描 + Stoplist 過濾 → [[連結]]    │
│                                                     │
│  守護層   Git  (auto commit)                        │
│           ↓  版本快照 → 可一鍵 git checkout . 還原  │
└─────────────────────────────────────────────────────┘
```

---

## 二、核心腳本清單

### 1. `dharma_watcher.py` — 知識守護神

| 項目 | 說明 |
|------|------|
| 路徑 | `E:\vscode\AI知識庫\dharma_watcher.py` |
| 行數 | 196 行 |
| 技術 | Python `watchdog` 庫（事件驅動，非輪詢）|
| 監控目標 | 六大館別 `concepts/` + `inbox/` 共 12 個目錄 |
| 冷卻防抖 | 5 秒內多次新增，合併為一次觸發 |
| 自動 Git | 織網完成後自動 `git commit`，留下可追蹤快照 |

**監控館別**：AI 總論、AI 龍樹、AI 彌勒、AI 智者、AI 惠能、AI 善導

### 2. `synapse_weaver.py` v2.0 — 突觸縫合引擎

| 項目 | 說明 |
|------|------|
| 路徑 | `E:\vscode\AI知識庫\synapse_weaver.py` |
| 行數 | 137 行 |
| 升級重點 | 防「藍色海洋」：每檔每概念只連結第一次出現 |
| Stoplist | 7 個歧義詞排除（心、道、法、性、觀、止、定）|
| Layer 1 定錨 | 自動掃描 AI 總論，確保新節點在正確義理座標落點 |
| 別名支援 | 讀取 YAML `aliases` 欄位，多名稱皆可觸發連結 |

---

## 三、基礎設施建置紀錄

### 3.1 Git 版本控制

```bash
# 初始化位置
E:\vscode\AI知識庫\數位佛堂\

# 重要 commits
[1] chore: initial commit - 數位佛堂 知識圖譜建立
[2] chore: add .gitignore - 排除 AI 快取與系統雜訊
[3] auto: synapse weave @ <timestamp>   ← 每次織網自動產生
```

**回滾指令**（若 AI 誤連大片文字）：
```bash
git -C "E:\vscode\AI知識庫\數位佛堂" checkout .
# 或針對單一 commit 回滾：
git -C "E:\vscode\AI知識庫\數位佛堂" revert HEAD
```

### 3.2 `.gitignore` 結界

排除以下雜訊，確保 Git 歷史只記錄「義理增量」：

```
.obsidian/workspace.json        # 視窗佈局（因人而異）
.obsidian/workspace-mobile.json
.obsidian/graph.json
.smart-env/                     # Smart Connections AI 快取
.obsidian/plugins/copilot/cache/
.DS_Store / Thumbs.db           # 系統雜訊
*.log / dharma_watcher.pid      # 執行日誌
```

### 3.3 Windows 工作排程器 — 守護神常駐

| 項目 | 設定 |
|------|------|
| 任務名稱 | `DharmaWatcher` |
| 觸發條件 | 使用者登入時自動啟動 |
| 執行層級 | 最高權限 (Highest) |
| 異常重啟 | 最多 3 次，間隔 1 分鐘 |
| 執行時限 | 無限制（長期守護）|

**管理指令**：
```powershell
# 手動啟動
Start-ScheduledTask -TaskName 'DharmaWatcher'

# 手動停止
Stop-ScheduledTask -TaskName 'DharmaWatcher'

# 查看狀態
Get-ScheduledTask -TaskName 'DharmaWatcher' | Select-Object TaskName, State

# 移除任務
Unregister-ScheduledTask -TaskName 'DharmaWatcher' -Confirm:$false
```

---

## 四、日常操作 SOP

### 新增知識節點（標準流程）

```
1. 與 AI 深度研討，產生頓悟或跨館關聯
   ↓
2. 整理成原子化 .md 檔案（依系統戒律格式）
   ↓
3. 存入 /inbox/（人工審閱區）
   ↓
4. 在 Obsidian 閱覽，確認義理正確，微調 aliases
   ↓
5. 拖入 /concepts/（正式入庫）
   ↓
6. dharma_watcher 自動偵測（< 5 秒）
   ↓
7. synapse_weaver 自動全庫織網
   ↓
8. Git 自動 commit（留下快照）
```

### NotebookLM 更新 SOP

每次織網完成後，需更新 NotebookLM 對照組：
1. 確認 Git 已 commit（`git log --oneline -3`）
2. 將各館 `wiki/concepts/` 下的 .md 檔案重新上傳至 LM 來源
3. 建議批次上傳策略：以館別為單位，一次上傳一館

---

## 五、依賴環境

| 套件 | 用途 | 安裝指令 |
|------|------|---------|
| `watchdog` | 檔案系統即時監控 | `pip install watchdog` |
| `PyYAML` | 讀取 Frontmatter aliases | `pip install pyyaml` |
| `git` | 版本控制與自動 commit | 系統已安裝 |
| `python 3.x` | 執行環境 | 系統已安裝 |

---

## 六、系統哲學

> **傳統 RAG**：死資料、活檢索
> **數位佛堂 v2.1**：活資料、導向式檢索

每一次與 AI 的深度研討，都會變成一條新的「突觸」。
隨著時間推移，知識圖譜對佛法的理解將越來越接近架構師的思維模式。

這不是在做工具，而是在鑄造一個「**數位法身**」。

「**法不孤起，仗境方生。**」
境已備，守護神已常駐，每一念皆有跡可循。

---

## 七、版本歷程

| 版本 | 日期 | 里程碑 |
|------|------|--------|
| v0.2 Alpha | 早期 | 建立 32+ 原子節點，五大館初步織網 |
| v1.0 | 中期 | synapse_weaver.py v1.0 上線 |
| v2.0 | 近期 | 防藍海修復（count=1）、Stoplist、Layer 1 定錨 |
| **v2.1** | **2026-04-24** | **閉環機制完整就緒：watcher + Git + 工作排程器** |

---

*本文件由架構師 Jimmy 與 AI 協作建置，記錄於 2026-04-24。*
