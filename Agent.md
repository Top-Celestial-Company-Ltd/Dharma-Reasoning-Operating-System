# 數位佛堂 - 核心憲法 (The Constitution)

> **[CAUTION] 最高指令**
> 本文件是「數位佛堂」專案的唯一最高規範。所有 AI Agent、腳本、與人工操作必須無條件遵守。
> 若本文件與系統中任何其他文件（包含 `.clinerules.md`、`系統戒律.md`、舊版建置報告）發生衝突，**一律以本文件為準**。

---

## 第一章：資料主權與聖地保護

### 1.1 唯一不可侵犯之母本
*   **絕對路徑**：`E:\RAG資料清洗\大覺藏集2026-01\大覺藏集`
*   **操作鐵律**：此目錄為「大覺藏原稿」唯讀聖地。僅允許「單向複製 (Copy)」動作。
*   **嚴禁事項**：絕對禁止在此目錄內進行任何寫入、修改、刪除、重命名或編碼轉換。

### 1.2 絕對的編碼清淨
*   **全鏈路 UTF-8**：數位佛堂內所有檔案必須 100% 採用 UTF-8 編碼。
*   **強制宣告**：所有讀寫操作必須明示指定 `encoding='utf-8'`，嚴禁依賴系統預設 (CP950/Big5)。
*   **歷史教訓**：2026 年曾因編碼錯誤導致 1,747 個節點全部淪為幻覺，被迫歸零重建。**編碼錯誤在 AI 系統中是不可逆的污染。**

---

## 第二章：三層資料管線架構

數位佛堂嚴格遵守「三階層資料架構」，AI 不可越權修改：

```
大覺藏 (唯讀母本)
  ↓ 單向複製
預上傳區 (Staging Area) ← dharma_chunker.py 物理切割
  ↓ AI 讀取
數位佛堂/[館名]/
  ├── raw/              ← 母本區 (純淨經文，AI 嚴禁修改)
  ├── wiki/
  │   ├── sources/      ← 導讀區 (sutra_weaver 生成的白話摘要)
  │   ├── concepts/     ← 名相區 (dharma_miner + refiner 萃取的原子節點) ★核心產出
  │   ├── bridges/      ← 映射區 (跨界數位映射)
  │   └── inbox/        ← 收件匣 (待處理項目)
  └── ...
```

### 2.1 地基污染原則
若 `raw/` 遭污染（編碼錯亂、內容截斷），必須假設所有下游 AI 產物皆已受污染。**打掉重練，不可修補。**

---

## 第三章：原子節點格式規範 (★ 最重要)

### 3.1 Metadata 格式 (嚴禁使用 YAML Frontmatter)

每個 `wiki/concepts/` 下的 `.md` 檔案，頂部**必須**使用以下自定義 Markdown 列表格式：

```markdown
##- **層級**: 2
- **宗派**: ["中觀", "天台"]
- **標籤**: ["天台宗", "判教"]
- **來源**: 001.《法華經祕釋》
- **狀態**: verified


# 名相標題

## 1. 來源
從經典《001.《法華經祕釋》》中自動精煉之原子名相。

> [!NOTE] 核心義理 (001.《法華經祕釋》)
> 不多於 30 字的精煉學術定義。
```

> **[WARNING] 絕對禁止使用 `---` YAML Frontmatter**
> 系統中的 Python 解析腳本（synapse_weaver、gemini_proxy 等）依賴 `##- **層級**:` 這個特殊標記來抓取節點屬性。使用 YAML 會導致全系統解析失敗。

### 3.2 知識圖譜三層分類 (Layer Hierarchy)

在生成或修改名相節點時，**必須**根據以下定義精確判定層級：

| Layer | 名稱 | 定義 | 範例 |
|:---:|:---|:---|:---|
| **1** | 核心層 | 佛教最根本的底層真理與教義 | 空性、真如、實相、四聖諦、無明、菩提、緣起、無我 |
| **2** | 衍伸層 | 特定宗派的核心理論或重要修行法門 | 一念三千、金剛界、胎藏界、十善業、四曼、止觀、三密加持 |
| **3** | 實體層 | 專有名詞、特定名物、地點、人物、譬喻 | 大日覺王、舍利弗、王舍城、善巧化城、醍醐、鹿車 |

### 3.3 增量知識累積法則 (Compounding Knowledge)

> **[IMPORTANT] 絕對禁止覆蓋已存在的節點**

當系統嘗試寫入一個已存在的名相檔案時：
*   **禁止**：以 `open(filepath, "w")` 覆蓋整個檔案。
*   **必須**：以 `open(filepath, "a")` 追加增量知識，格式為：
    ```markdown
    > [!NOTE] 增量感悟 (來源名稱)
    > 新的補充定義或不同經典的詮釋角度。
    ```
*   **原理**：知識應像滾雪球一樣越滾越大。第一次寫入的定義是「骨架」，後續每次碰到同一個名相，都應該從不同角度豐富它。

## 第四章：認識論斷路器 (Epistemological Circuit Breaker)

### 4.1 立場防護機制
**[MANDATORY]** 當系統調用「如來藏 (Tathagatagarbha)」或相關「真心、真如、本覺、大乘起信論」節點進行推論時，必須自動啟動以下防護邏輯：
- **如來藏防護**：涉及「真心、本覺」時，啟動「非造物主」咒語（此動能乃法爾如是之性德顯現...）。
- **中觀防護**：涉及「空、無自性」時，啟動「非虛無主義」咒語（此空乃自性空，非斷滅空...）。
- **唯識防護**：涉及「唯識、阿賴耶識、四分、三自性」時，啟動「非唯我論」咒語：「此『唯識』乃『離外境之識』，非『唯我論 (Solipsism)』。絕非否定他心或其他有情之存在，而是強調一切所緣皆不離識之顯現。」
- **目的**：嚴防系統墮入「常、斷、唯我」三種常見偏差，確保義理對齊的純淨度。

---

## 第五章：AI 呼叫規範 (SDK Mandate)

### 4.1 雙軌分工法則

本系統存在兩條完全獨立的 AI 呼叫路線，**嚴禁混用**：

| 路線 | 用途場景 | 呼叫方式 | 模型 |
|:---|:---|:---|:---|
| **工廠量產線** | Stage-1 採礦、Stage-2 精煉、synapse_weaver 交叉連結 | Python SDK (`google.generativeai`) + API Key | `gemini-2.5-flash-lite` |
| **問答互動線** | Obsidian Copilot 即時問答、人工深度覆寫 | `gemini_proxy.py` → CLI + OAuth | `auto` (由 Gemini CLI 自動路由) |

**工廠量產線規則**：
*   全部使用 Python SDK 直接呼叫 API，**嚴禁**使用 `subprocess.Popen` 呼叫 `gemini.cmd`。
*   品質控制依賴**嚴格的 Prompt 約束 (Constraint Engineering)**，而非模型智商。
*   歷史教訓：CLI 方案曾導致 JSON 解析失敗、`Not enough arguments` 錯誤、cp950 編碼崩潰。

**問答互動線規則**：
*   `gemini_proxy.py` 為 Obsidian Copilot 提供即時問答服務，走 CLI + OAuth 調用使用者個人的 Gemini 額度。
*   此路線**不消耗 API Key 配額**，與工廠量產線互不干擾。

### 4.2 API 安全防護
*   **配額防護**：所有腳本必須攔截 `429 Resource Exhausted` 並優雅終止。
*   **節律控制**：批次間必須包含 `time.sleep()` 呼吸節奏（建議 ≥ 15 秒）。
*   **單一引擎原則**：同一時間只允許一台 AI 引擎消耗 API 配額。禁止 refiner 與 synapse_weaver 同時運行。

---

## 第五章：核心腳本清單與職責

| 腳本 | 版本 | 職責 | 輸入 | 輸出 |
|:---|:---:|:---|:---|:---|
| `dharma_chunker.py` | - | 將巨型經文物理切割為小檔案 | `raw/` 大檔案 | `預上傳區/` 小檔案 |
| `dharma_factory_v7.py` | v7.5 | Stage-1 粗篩：從經文中提取候選詞彙 | `預上傳區/` | `pending_refinement.txt` |
| `dharma_refiner_v1.py` | v2.0 | Stage-2 精煉：定義 + 分層 + 寫入原子節點 | `pending_refinement.txt` | `wiki/concepts/*.md` |
| `sutra_weaver.py` | v3.1 | 生成經文導讀頁 (白話摘要 + 關鍵詞) | `預上傳區/` | `wiki/sources/*.md` |
| `synapse_weaver.py` | v4.0 | 交叉連結引擎：注入 `[[雙向連結]]` + 增量感悟 | `wiki/concepts/` | 修改同目錄檔案 |
| `dharma_monitor.py` | v1.0 | 每 30 分鐘回報產線進度 | 讀取檔案系統 | `dharma_monitor.log` |
| `gemini_proxy.py` | v4.0 | Obsidian Copilot 的 Agentic Gateway | 使用者提問 | AI 回覆 |

---

## 第六章：預上傳管線 (Staging Pipeline)

為避免 AI 因巨型經典導致記憶體耗盡或認知失焦：
1.  **物理隔離**：原典必須先從 `raw/` 複製到 `預上傳區 (E:\vscode\AI知識庫\預上傳區)`。
2.  **實體切割**：使用 `dharma_chunker.py` 依卷/品切割為數千字小檔案。
3.  **單向處理**：AI 腳本僅讀取預上傳區，產出寫入 `數位佛堂/wiki/`。

---

## 第七章：五大主題館

| 主題館 | 核心導師 | 核心教義 | 路徑 |
|:---|:---|:---|:---|
| AI 總論 | (跨宗派基礎) | 佛法共通真理 | `數位佛堂/AI 總論/` |
| AI 龍樹 | 龍樹菩薩 | 中觀、般若、空性 | `數位佛堂/AI 龍樹/` |
| AI 彌勒 | 彌勒菩薩 | 唯識、心理、阿賴耶 | `數位佛堂/AI 彌勒/` |
| AI 智者 | 智者大師 | 天台、圓融、法華 | `數位佛堂/AI 智者/` |
| AI 惠能 | 惠能大師 | 禪宗、頓悟、見性 | `數位佛堂/AI 惠能/` |
| AI 善導 | 善導大師 | 淨土、信願、念佛 | `數位佛堂/AI 善導/` |

---

## 第八章：交叉連結與知識圖譜

### 8.1 雙向連結規範
*   所有出現在文中的佛學名相，若在 `wiki/concepts/` 中已有對應檔案，**必須**使用 `[[名相]]` 語法建立連結。
*   連結數量建議：每個節點 3-8 個有效連結。
*   `synapse_weaver.py` 負責自動掃描並注入連結。

### 8.2 增量感悟格式
當 AI 在處理經文時發現對某個已存在名相有更好的解釋：
```markdown
> [!NOTE] 增量感悟 (來源經文或節點名稱)
> 新的補充解釋，不超過 100 字。
```

---

## 第九章：環境防禦 (Runtime Safety)

1.  **單一實例鎖**：長期運算腳本必須使用 `.lock` 檔案確保不重複啟動。
2.  **僵屍清除**：啟動前清理殘留的 `node.exe` 進程。
3.  **同步執行優先**：必須保持前景輸出，確保可隨時 `Ctrl+C` 中止。
4.  **行動前局部測試**：批次腳本必須先對 1-2 個檔案試跑，確認無誤後才全量執行。

---

## 第十章：設計哲學 (LLM Wiki + Graphify)

本系統的設計靈感來自 **Karpathy LLM Wiki** 與 **Graphify Knowledge Graph**：
*   **Karpathy**：人類可讀的 Markdown Wiki、持久化純文本、Git 版本控制、Schema 治理。
*   **Graphify**：Ingest 預先綜合（不依賴即時 RAG）、Compounding Knowledge（知識累積效應）、交叉連結圖譜。
*   **我們的超越**：Layer 1/2/3 三層立體知識架構、多館宗派分治、佛法領域深度定製的 Prompt Constraint Engineering。

---

*「以大模型之電力，轉動數位法輪；以純文本之永恆，傳遞不二法門。」*
**憲法版本**：v4.0 (2026-05-01) | **狀態**：🕉️ 運作圓滿
